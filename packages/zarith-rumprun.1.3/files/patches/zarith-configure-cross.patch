--- zarith.1.3/configure	2013-05-21 12:24:01.000000000 +0200
+++ zarith-rumprun.1.3/configure	2015-06-11 15:23:15.828171850 +0200
@@ -245,11 +245,12 @@
 
 hasdynlink='no'
 
-if test $hasocamlopt = yes
-then
-    checkcmxalib dynlink.cmxa
-    if test $? -eq 1; then hasdynlink='yes'; fi
-fi
+# XXX Not for rumprun
+# if test $hasocamlopt = yes
+# then
+#     checkcmxalib dynlink.cmxa
+#     if test $? -eq 1; then hasdynlink='yes'; fi
+# fi
 
 
 # installation method
@@ -389,11 +390,11 @@
 # generated by ./configure
 
 CC=$cc
-OCAMLC=$ocamlc
-OCAMLOPT=$ocamlopt
-OCAMLDEP=$ocamldep
-OCAMLMKLIB=$ocamlmklib
-OCAMLDOC=$ocamldoc
+OCAMLC=ocamlfind $ocamlc
+OCAMLOPT=ocamlfind $ocamlopt
+OCAMLDEP=ocamlfind $ocamldep
+OCAMLMKLIB=ocamlfind $ocamlmklib
+OCAMLDOC=ocamlfind $ocamldoc
 OCAMLFLAGS=$mlflags
 OCAMLOPTFLAGS=$mloptflags
 OCAMLINC=$mlinc
